// Layer 3 — individual lead detail + action buttons
// TODO §9
export default function LeadDetailPage() {
  return <main className="flex h-screen items-center justify-center text-muted-foreground text-sm">Lead detail — coming in §9</main>;
}
