// Layer 2 — live pipeline graph + HITL panel
// TODO §5: implement pipeline-graph.tsx + hitl-panel.tsx
export default function PipelinePage() {
  return (
    <main className="text-muted-foreground flex h-screen items-center justify-center text-sm">
      Pipeline — coming in §5
    </main>
  );
}
