// Layer 2 — multi-file drop zone + pipeline trigger
// TODO §5: implement upload UI
export default function UploadPage() {
  return (
    <main className="text-muted-foreground flex h-screen items-center justify-center text-sm">
      Upload — coming in §5
    </main>
  );
}
