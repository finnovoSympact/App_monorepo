// Sanad Passport — printable, bank-grade layout
// TODO §6: render signed passport with P&L table, score dial, source-trace, signature footer
export default function PassportPage() {
  return <main className="flex h-screen items-center justify-center text-muted-foreground text-sm">Passport — coming in §6</main>;
}
